import React from 'react';

import Form from '../Form/Form';

const Write = () => {
    return (
        <div className="container">
            <Form />
        </div>
    );
};

export default Write;